<view class="device-container">
  <!-- 顶部操作栏 -->
  <view class="header-bar">
    <text class="page-title">设备管理</text>
    <button class="btn-add" bindtap="goToBind">+ 绑定新设备</button>
  </view>

  <!-- 设备列表 -->
  <view wx:if="{{devices.length > 0}}" class="device-list">
    <view wx:for="{{devices}}" wx:key="device_id" class="device-card">
      <view class="card-header">
        <view class="device-info">
          <view class="device-icon">🪞</view>
          <view class="device-meta">
            <text class="device-name">{{item.device_name}}</text>
            <text class="device-id">ID: {{item.device_id}}</text>
          </view>
        </view>
        <view class="status-badge {{item.status === 'online' ? 'online' : 'offline'}}">
          {{item.status === 'online' ? '在线' : '离线'}}
        </view>
      </view>
      
      <view class="card-body">
        <view class="data-row">
          <view class="data-item">
            <text class="data-label">{{item.primaryLabel}}</text>
            <text class="data-value">{{item.primaryValue}}</text>
          </view>
          <view class="data-item">
            <text class="data-label">{{item.secondaryLabel}}</text>
            <text class="data-value">{{item.secondaryValue}}</text>
          </view>
        </view>
        
        <view class="data-row">
          <view class="data-item">
            <text class="data-label">{{item.thirdLabel}}</text>
            <text class="data-value">{{item.thirdValue}}</text>
          </view>
          <view class="data-item">
            <text class="data-label">{{item.fourthLabel}}</text>
            <text class="data-value">{{item.fourthValue}}</text>
          </view>
        </view>
      </view>
      
      <view class="card-footer">
        <button class="btn-detail" data-id="{{item.device_id}}" bindtap="viewDeviceDetail">查看详情</button>
        <button class="btn-delete" data-id="{{item.device_id}}" data-name="{{item.device_name}}" bindtap="deleteDevice">删除</button>
      </view>
    </view>
  </view>

  <!-- 空状态 -->
  <view wx:else class="empty-state">
    <view class="empty-icon">📱</view>
    <text class="empty-text">暂无设备</text>
    <text class="empty-desc">点击右上角按钮绑定您的第一台设备</text>
    <button class="btn-primary" bindtap="goToBind">立即绑定</button>
  </view>
</view>
